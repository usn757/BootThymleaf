spring:
  application:
    name: BootThymeleafApp
#  mvc:
#    view:
#      prefix: /WEB-INF/views/
#      suffix: .jsp
  profiles:
    active: dev
  # JPA
  datasource:
    # JPA? ?? ???? ????? DB ??? ??? ? ???? ??? ??
    # ???? ?? ?? ????? ???...
    # ?? ??? MySQL? ??? ????...
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      #      ddl-auto: update # ?? ????? create-drop, ?? ????? validate ??
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true # ?? ???? SQL ?? ??
    properties.hibernate.format_sql: true